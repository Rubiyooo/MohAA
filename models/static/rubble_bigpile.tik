TIKI
setup
{
	scale 0.52			// Set default scale to 16/30.5 since world is in 16 units per foot and model is in cm's
	//scale 1.33			// Convert from inches to game units
	path models/static/rubble
	skelmodel rubble_bigpile.skb
	surface lrubble1 shader static_lrubble1
	surface boards shader static_boards
}

animations
{
	idle     rubble_bigpile.skc
}

/*QUAKED static_obstacle_rubble-bigpile (0.5 0.0 0.5) (-32 -32 0) (32 32 128)
*/