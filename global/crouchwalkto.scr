// parameters:
// local.node - node to crouchwalk to

start local.node:

if(self.health <= 0)
{
	end
}


//self.position = "crouchwalk" // temporary to make it work
self exec global/moveto.scr anim/crouch_walk.scr local.node
